export interface Cliente {
    id: string
    nome: string
    telefone: string
    idade: number
    email: string
}

export interface CriarCliente {
    nome: string
    telefone: string
    idade: number
    email: string
}